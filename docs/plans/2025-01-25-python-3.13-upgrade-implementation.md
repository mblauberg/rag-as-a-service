# Python 3.13 Upgrade Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Upgrade all Python services from Python 3.11 to Python 3.13

**Architecture:** Update Docker base images and Poetry Python version constraints across three microservices (API, Embedder, Generator). Update key dependencies (FastAPI, Pydantic) for Python 3.13 compatibility. Verify all tests pass.

**Tech Stack:** Python 3.13, Docker, Poetry, FastAPI, Pydantic, SQLAlchemy, PyTorch

---

## Task 1: Update API Service to Python 3.13

**Files:**
- Modify: `.worktrees/python-3.13-upgrade/services/api/Dockerfile:1`
- Modify: `.worktrees/python-3.13-upgrade/services/api/pyproject.toml:9`

**Step 1: Update API Dockerfile base image**

Edit `.worktrees/python-3.13-upgrade/services/api/Dockerfile`:

```dockerfile
FROM python:3.13-slim
```

Change line 1 from `FROM python:3.11-slim` to `FROM python:3.13-slim`

**Step 2: Update API pyproject.toml Python version**

Edit `.worktrees/python-3.13-upgrade/services/api/pyproject.toml`:

```toml
python = "^3.13"
```

Change line 9 from `python = "^3.11"` to `python = "^3.13"`

**Step 3: Update FastAPI and Pydantic versions**

Edit `.worktrees/python-3.13-upgrade/services/api/pyproject.toml`:

```toml
fastapi = "^0.120.0"
pydantic = "^2.12.0"
```

Change line 10 from `fastapi = "^0.109.0"` to `fastapi = "^0.120.0"`
Change line 14 from `pydantic = "^2.5.3"` to `pydantic = "^2.12.0"`

**Step 4: Regenerate Poetry lock file**

```bash
cd .worktrees/python-3.13-upgrade/services/api
poetry lock --no-update
```

Expected: Lock file regenerated successfully with Python 3.13 constraints

**Step 5: Commit API service changes**

```bash
cd .worktrees/python-3.13-upgrade
git add services/api/Dockerfile services/api/pyproject.toml services/api/poetry.lock
git commit -m "feat(api): upgrade to Python 3.13 and update dependencies

- Update Dockerfile to python:3.13-slim
- Update pyproject.toml Python constraint to ^3.13
- Upgrade FastAPI to ^0.120.0 for Python 3.13 support
- Upgrade Pydantic to ^2.12.0 for better 3.13 compatibility

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 2: Update Embedder Service to Python 3.13

**Files:**
- Modify: `.worktrees/python-3.13-upgrade/services/embedder/Dockerfile:1`
- Modify: `.worktrees/python-3.13-upgrade/services/embedder/pyproject.toml:9`

**Step 1: Update Embedder Dockerfile base image**

Edit `.worktrees/python-3.13-upgrade/services/embedder/Dockerfile`:

```dockerfile
FROM python:3.13-slim
```

Change line 1 from `FROM python:3.11-slim` to `FROM python:3.13-slim`

**Step 2: Update Embedder pyproject.toml Python version**

Edit `.worktrees/python-3.13-upgrade/services/embedder/pyproject.toml`:

```toml
python = "^3.13"
```

Change line 9 from `python = "^3.11"` to `python = "^3.13"`

**Step 3: Update FastAPI and Pydantic versions**

Edit `.worktrees/python-3.13-upgrade/services/embedder/pyproject.toml`:

```toml
fastapi = "^0.120.0"
pydantic = "^2.12.0"
```

Change line 10 from `fastapi = "^0.109.0"` to `fastapi = "^0.120.0"`
Change line 12 from `pydantic = "^2.5.3"` to `pydantic = "^2.12.0"`

**Step 4: Regenerate Poetry lock file**

```bash
cd .worktrees/python-3.13-upgrade/services/embedder
poetry lock --no-update
```

Expected: Lock file regenerated successfully with Python 3.13 constraints

**Step 5: Commit Embedder service changes**

```bash
cd .worktrees/python-3.13-upgrade
git add services/embedder/Dockerfile services/embedder/pyproject.toml services/embedder/poetry.lock
git commit -m "feat(embedder): upgrade to Python 3.13 and update dependencies

- Update Dockerfile to python:3.13-slim
- Update pyproject.toml Python constraint to ^3.13
- Upgrade FastAPI to ^0.120.0 for Python 3.13 support
- Upgrade Pydantic to ^2.12.0 for better 3.13 compatibility

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 3: Update Generator Service to Python 3.13

**Files:**
- Modify: `.worktrees/python-3.13-upgrade/services/generator/Dockerfile:1`
- Modify: `.worktrees/python-3.13-upgrade/services/generator/pyproject.toml:9`

**Step 1: Update Generator Dockerfile base image**

Edit `.worktrees/python-3.13-upgrade/services/generator/Dockerfile`:

```dockerfile
FROM python:3.13-slim
```

Change line 1 from `FROM python:3.11-slim` to `FROM python:3.13-slim`

**Step 2: Update Generator pyproject.toml Python version**

Edit `.worktrees/python-3.13-upgrade/services/generator/pyproject.toml`:

```toml
python = "^3.13"
```

Change line 9 from `python = "^3.11"` to `python = "^3.13"`

**Step 3: Update FastAPI and Pydantic versions**

Edit `.worktrees/python-3.13-upgrade/services/generator/pyproject.toml`:

```toml
fastapi = "^0.120.0"
pydantic = "^2.12.0"
```

Change line 10 from `fastapi = "^0.109.0"` to `fastapi = "^0.120.0"`
Change line 12 from `pydantic = "^2.5.3"` to `pydantic = "^2.12.0"`

**Step 4: Regenerate Poetry lock file**

```bash
cd .worktrees/python-3.13-upgrade/services/generator
poetry lock --no-update
```

Expected: Lock file regenerated successfully with Python 3.13 constraints

**Step 5: Commit Generator service changes**

```bash
cd .worktrees/python-3.13-upgrade
git add services/generator/Dockerfile services/generator/pyproject.toml services/generator/poetry.lock
git commit -m "feat(generator): upgrade to Python 3.13 and update dependencies

- Update Dockerfile to python:3.13-slim
- Update pyproject.toml Python constraint to ^3.13
- Upgrade FastAPI to ^0.120.0 for Python 3.13 support
- Upgrade Pydantic to ^2.12.0 for better 3.13 compatibility

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 4: Rebuild Docker Containers and Verify

**Files:**
- Test: All Docker containers build successfully
- Test: All services start without errors

**Step 1: Stop existing containers**

```bash
cd .worktrees/python-3.13-upgrade
docker-compose -f infrastructure/docker-compose/docker-compose.yml down
```

Expected: All containers stopped and removed

**Step 2: Rebuild all containers with Python 3.13**

```bash
cd .worktrees/python-3.13-upgrade
docker-compose -f infrastructure/docker-compose/docker-compose.yml build
```

Expected: All 4 services (api, embedder, generator, frontend) build successfully
Watch for: Python 3.13 in build logs, FastAPI 0.120.x installation

**Step 3: Start containers**

```bash
cd .worktrees/python-3.13-upgrade
docker-compose -f infrastructure/docker-compose/docker-compose.yml up -d
```

Expected: All containers start and reach healthy status

**Step 4: Verify containers are running**

```bash
docker-compose -f infrastructure/docker-compose/docker-compose.yml ps
```

Expected: All services show "Up" status

**Step 5: Check logs for startup errors**

```bash
docker-compose -f infrastructure/docker-compose/docker-compose.yml logs api | tail -20
docker-compose -f infrastructure/docker-compose/docker-compose.yml logs embedder | tail -20
docker-compose -f infrastructure/docker-compose/docker-compose.yml logs generator | tail -20
```

Expected: No errors, services listening on expected ports

---

## Task 5: Run Test Suites

**Files:**
- Test: `services/api/tests/`
- Test: `services/embedder/tests/`
- Test: `services/generator/tests/`
- Test: `services/frontend/src/`

**Step 1: Run API tests**

```bash
cd .worktrees/python-3.13-upgrade/services/api
poetry run pytest --tb=short -v
```

Expected: All tests pass
Watch for: SyntaxWarnings (acceptable), test failures (not acceptable)

**Step 2: Run Embedder tests**

```bash
cd .worktrees/python-3.13-upgrade/services/embedder
poetry run pytest --tb=short -v
```

Expected: All tests pass

**Step 3: Run Generator tests**

```bash
cd .worktrees/python-3.13-upgrade/services/generator
poetry run pytest --tb=short -v
```

Expected: All tests pass

**Step 4: Run Frontend tests**

```bash
cd .worktrees/python-3.13-upgrade/services/frontend
npm test -- --run
```

Expected: All tests pass

**Step 5: Commit test verification results**

Create a test results summary file:

```bash
cd .worktrees/python-3.13-upgrade
cat > docs/plans/2025-01-25-test-results.md << 'EOF'
# Python 3.13 Upgrade Test Results

**Date:** 2025-01-25
**Python Version:** 3.13

## Test Results

- API Service: ✓ All tests passing
- Embedder Service: ✓ All tests passing
- Generator Service: ✓ All tests passing
- Frontend Service: ✓ All tests passing

## Warnings

[List any SyntaxWarnings or deprecation warnings here]

## Container Status

All containers running successfully on Python 3.13.
EOF

git add docs/plans/2025-01-25-test-results.md
git commit -m "test: verify Python 3.13 upgrade test results

All test suites passing after Python 3.13 upgrade.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Task 6: Manual Smoke Testing

**Files:**
- Test: Full workflow from frontend

**Step 1: Verify frontend loads**

Open browser to http://localhost:3000
Expected: Frontend loads without errors

**Step 2: Upload a test document**

Action: Upload a .txt or .pdf document via the UI
Expected: Document uploads successfully, appears in documents list

**Step 3: Search for content**

Action: Enter a search query related to uploaded document
Expected: Search returns relevant results

**Step 4: Generate a response**

Action: Use RAG feature to generate response based on uploaded documents
Expected: Response generated successfully using document context

**Step 5: Document smoke test results**

```bash
cd .worktrees/python-3.13-upgrade
cat >> docs/plans/2025-01-25-test-results.md << 'EOF'

## Manual Smoke Test Results

- Frontend loads: ✓
- Document upload: ✓
- Search functionality: ✓
- RAG generation: ✓

All core workflows functioning correctly with Python 3.13.
EOF

git add docs/plans/2025-01-25-test-results.md
git commit -m "test: complete manual smoke testing for Python 3.13

All core workflows verified working.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

Co-Authored-By: Claude <noreply@anthropic.com>"
```

---

## Verification Checklist

After completing all tasks, verify:

- [ ] All Dockerfiles updated to python:3.13-slim
- [ ] All pyproject.toml files specify python = "^3.13"
- [ ] FastAPI upgraded to ^0.120.0 in all services
- [ ] Pydantic upgraded to ^2.12.0 in all services
- [ ] Poetry lock files regenerated
- [ ] All Docker containers build successfully
- [ ] All containers start without errors
- [ ] API tests pass
- [ ] Embedder tests pass
- [ ] Generator tests pass
- [ ] Frontend tests pass
- [ ] Document upload works
- [ ] Search works
- [ ] RAG generation works
- [ ] All changes committed with descriptive messages

## Rollback Plan

If critical issues arise:

```bash
# Return to master branch
cd /Users/user/Documents/01_Active/UQ/INFS3208/Individual\ Project/raas
git checkout master

# Remove worktree
git worktree remove .worktrees/python-3.13-upgrade

# Rebuild containers on Python 3.11
docker-compose -f infrastructure/docker-compose/docker-compose.yml down
docker-compose -f infrastructure/docker-compose/docker-compose.yml up --build -d
```

## Next Steps After Completion

1. Merge upgrade/python-3.13 branch to master
2. Deploy to production
3. Monitor logs for 24-48 hours for any Python 3.13-specific issues
4. Update project documentation with Python 3.13 requirement

apiVersion: v1
kind: Service
metadata:
  name: generator
  namespace: raas
  labels:
    app: generator
spec:
  type: ClusterIP
  ports:
  - port: 8002
    targetPort: 8002
    protocol: TCP
    name: http
  selector:
    app: generator

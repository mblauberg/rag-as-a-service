apiVersion: v1
kind: Service
metadata:
  name: embedder
  namespace: raas
  labels:
    app: embedder
spec:
  type: ClusterIP
  ports:
  - port: 8001
    targetPort: 8001
    protocol: TCP
    name: http
  selector:
    app: embedder

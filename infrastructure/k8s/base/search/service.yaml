apiVersion: v1
kind: Service
metadata:
  name: search
  namespace: raas
  labels:
    app: search
spec:
  type: ClusterIP
  ports:
  - port: 8003
    targetPort: 8003
    protocol: TCP
    name: http
  selector:
    app: search
